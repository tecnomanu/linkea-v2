/**
 * Default values and constants for the application.
 *
 * Note: All data should come from backend services/controllers.
 * Empty states should be handled with empty arrays [], not mock data.
 */

// Empty - reserved for future default configs if needed
